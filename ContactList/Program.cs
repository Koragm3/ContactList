using ContactList.Models;
using ContactList.Services;
using System.Text.Json.Serialization;

// Runing the app menu by calling the method MenuOptions 
MenuNav menu = new MenuNav();
menu.MenuOptions();
